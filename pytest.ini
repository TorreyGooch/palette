[pytest]
testpaths = tests
# tests/ is on the path so conftest helpers import cleanly, and the repo root
# so palette_app/ and quotesource/ resolve without an install step.
pythonpath = . tests
addopts = -q
